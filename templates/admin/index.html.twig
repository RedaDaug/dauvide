{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <table class="table">
        <tr>
            <th></th>
            <th>Produkto pavadinimas</th>
            <th></th>
        </tr>
        {% for product in products %}
            <tr>
                <td><img src="{{ asset('img/' ~ product.imageName) }}" id="currentImg" alt="Teacup" title="Blue Teacup" width="40px"></td>
                <td>{{ product.name }}</td>
{#                <td><a href="{{ path('product.edit', {id: product.id}) }}">Edit</a>  | <a onclick="return confirm('Ar tikrai norite istrinti?')" href="{{ path('product.delete', {id: product.id}) }}">Delete</a>#}
                </td>
            </tr>
        {% endfor %}
    </table>
</div>
{% endblock %}
