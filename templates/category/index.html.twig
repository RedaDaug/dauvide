{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
    <section id="produktai" >
        <div class="container mt-5 pt-5 text-center">
            <h3 class="text-dark">Produktų kategorijos</h3>
            <div class="row mt-3 mb-5">
                {% for category in categories %}
                    <div class="col-lg-2 col-md-4 col-sm-6 col-6 text-center mt-2 mb-2">
{#                        <a href="{{ path('category.show', {id: category.id}, {category: category.name}) }}" role="button" class="mygas skalas" >#}
                            <div class="rounded p-0">

                                <img src=" {{ asset('img/' ~ category.imageName) }}" alt="purskiklis" width="100%" >
                                <div class="card-title ">{{ category.name}}</div>
                            </div>
                        </a>
                    </div>
                {% endfor %}

            </div>
        </div>
    </section>


{% endblock %}
