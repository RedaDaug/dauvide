{% extends 'base.html.twig' %}

{% block body %}
    <script type="text/javascript">

        function showImage(imgPath) {
            var curImage = document.getElementById('currentImg');
            curImage.src = imgPath;
        }
    </script>
    <section>
        <div class="container mt-5">
            {% for product in products|slice (0,1) %}
                <ul class="breadcrumb">
                    <li><a href="{{ path('home') }}">Pradžia</a></li>
                    <li><a href="{{ path('category.show', {id: product.mainProduct.category.id}) }}">{{ product.mainProduct.category }}</a></li>
                    <li>{{ product.mainProduct }}</li>
                </ul>
            {% endfor %}
        </div>

    </section>
    <section id="produktai-kategorijoje">
        <div class="container mt-1 pt-5">
            <div class="row">
                <div class="col-md-6">
                    {% for product in products|slice (0,1) %}
                        <div>
                            <img src="{{ asset('img/' ~ product.imageName) }}" id="currentImg" alt="Teacup" title="Blue Teacup" width="100%">
                        </div>
                    {% endfor %}
                    <ul class="colorpicker pl-0 row">
                        {% for product in products %}
                            <li class="col-lg-2 col-md-3 col-sm-4 col-4"><img src="{{ asset('img/' ~ product.imageName) }}" width="50%"
                                                            onclick="showImage('{{ asset('img/' ~ product.imageName) }}')" data-pin-nopin="true"></li>

                        {% endfor %}
                    </ul>
                </div>
                <div class="col-md-6">
                    {% for product in products|slice (0,1) %}
                        <h2 class="title"> {{ product.mainProduct }}</h2>
                        <a href="{{ path('category.show', {id: product.mainProduct.category.id}) }}">{{ product.mainProduct.category }}</a>
                        <p>{{ product.mainProduct.description }} </p>
                    {% endfor %}

                    {% for product in products %}
                        <li>{{ product.aroma }}</li>

                    {% endfor %}

                </div>

            </div>
        </div>

    </section>

    <section>
        {% for product in products|slice (0,1) %}
        {{ render(controller(
            'App\\Controller\\MainProductController::listMainProducts', {id: product.mainProduct.category.id})) }}
        {% endfor %}
    </section>




{% endblock %}